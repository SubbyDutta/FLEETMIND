[pytest]
; makes `from app...` imports resolve no matter where pytest is launched from
pythonpath = .
testpaths = tests
markers =
    integration: needs running Postgres with indexed runbooks + Gemini API key
