rootProject.name="fleetmind"

include(
        "common-events",
        "common-proto",
        "fleet-simulator",
        "stream-processor",
        "command-service",
        "query-service",
        "api-gateway",
        "notification-service"
)